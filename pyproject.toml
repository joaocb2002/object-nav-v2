[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "object-nav-v2"
version = "0.1.0"
description = "ObjectNav experiments and utilities."
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["object_nav*"]
